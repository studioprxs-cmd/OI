import { AdminPageSkeleton } from "@/components/admin/AdminPageSkeleton";

export default function Loading() {
  return <AdminPageSkeleton title="토픽 운영 데이터를 불러오는 중" subtitle="모바일 우선 운영 레이아웃을 준비하고 있습니다." />;
}
